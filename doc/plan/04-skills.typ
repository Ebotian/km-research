= 技能集

== 组织原则

技能是「判据与流程」的载体，不是文档仓库。调研给出三条硬约束：

- *description 只写触发条件与症状，绝不写步骤摘要*。否则模型照描述走而不读正文——这是「技能描述太泛导致不触发」与「描述即流程导致正文被跳过」两种失效的共同根源。
- *三层渐进披露加硬预算*。系统提示只放技能名、描述与路径（实测插件技能归入 `Extra` 段）；正文按需注入，单文件正文压在 500 行以内；深材料（定理库索引、基准套件参数、反例构造手册）放同级 `references/`，可执行脚本放同级 `scripts/`。*资源不被正文显式引用等于不存在*。
- *确定性逻辑不进技能*。凡是脚本能做的（形式化检查、跑分、结果解析）都放 `bin/` 下的命令或同级 `scripts/`，技能只写判据。

技能名一律带 `opl-` 前缀。原因是实测优先级为 内置 0 小于 插件 5 小于 `Extra` 10 小于 用户 20 小于 项目 30，即插件技能会被同名项目或用户技能静默覆盖。

== 技能清单

#table(
  columns: (auto, 1fr),
  table.header([*技能*], [*触发条件（写进 description 的内容）*]),
  [`opl-entry` `[已实现]`], [会话开始的总纲与路由。当任务涉及攻击一个猜想、寻找反例、排除某个有限域、复核一份证明、或跑算法实验时读它——它给出命令清单、退出码契约与三条不可协商的纪律],
  [`opl-formalize` `[已实现]`], [当需要把一个自然语言猜想转成可编译的 Lean 陈述时；当需要评估一份已有 Lean 陈述是否忠实于原命题时；当形式化状态要从 draft 推进到 compiles 时],
  [`opl-refute` `[已实现]`], [当需要为一个猜想寻找反例时；当需要排除某个有限域（断言「该范围内无反例」）时；当拿到一个疑似反例或一份证明需要独立复核时；当要把一次反例搜索的结论落进猜想台账时],
  [`opl-prove` `[已实现]`], [当需要在 Lean 中搜索一个证明时；当一份证明含 `sorry`、或依赖白名单外公理需要定位时；当需要判断一个 Lean 文件是否构成证明时],
  [`opl-evolve` `[已实现]`], [当需要为一个算法问题寻找更好的实现或上界、且存在确定性评估器时；当要把一份骨架的某个函数交给模型去改良时；当需要判断某个候选是否真的比现状更好时],
  [`opl-benchmark` `[未实现]`], [当需要比较两个算法或两组配置时；当需要对含超时实例的运行时间做统计时],
  [`opl-report` `[未实现]`], [当需要把一次或多次运行整理成可读报告时；当需要更新猜想台账状态时],
)

设计上由七个技能覆盖一条完整往返：`opl-entry` 路由，`opl-formalize` 与 `opl-refute` / `opl-prove` 构成攻防两侧，`opl-evolve` 负责算法上界改进，`opl-benchmark` 负责实证评估，`opl-report` 负责沉淀。*实现进度与原计划有差*：截至插件 `0.6.0` 只落地了前五个（`opl-entry` / `opl-refute` / `opl-formalize` / `opl-prove` / `opl-evolve`，即里程碑 M0–M4），`opl-benchmark`（M5）与 `opl-report`（M6）*未实现*——往返在「实证评估」与「沉淀」两段断开，这两段的结论目前只能手工整理。`opl-entry` 通过清单的 `sessionStart.skill` 挂载，作用是把「问题术语到本插件工具调用」的映射一次性注入，而不是每次对话重复解释。

== 各技能正文要点

`opl-entry` 是最短也最硬的一个。它声明退出码契约（六档 `0`–`5`，于是 `unknown` 与 `unsat` 在数值上就不可能混淆）、三条不可协商的纪律（`unknown` 绝不折叠成「已证」或「无反例」、结论必须经与生成路径不同的代码复核、缺验证器时硬失败），再给出任务到技能的映射表与命令速查。它不描述任何问题的解法。*实现现状*：那张映射表当前只列了一条——反例搜索类任务走 `opl-refute`，其余四个技能没有对应的路由行。

`opl-formalize` 的正文必须包含忠实度检查清单，因为这是自动化最薄弱的环节。清单至少覆盖：量词顺序与作用域、是否隐含了「非空」「有限」「非退化」等前提、`ℕ` 与 `ℤ` 的边界、以及收敛/极限的表述方式。技能须明确要求：形式化状态在人工确认前停在 `compiles`。

`opl-refute` 是使用频率最高的技能。它把一次反例搜索固定成五步：登记 → 编码并让两条独立路径互相证伪 → 搜索 → 复核见证（`sat` 侧）或复核证明（`unsat` 侧）→ 定案，而两支的定案命令不同——「找到反例」写成 `refuted`，「该域内无解」只能写成 `no_counterexample_in_range`。两项硬约束：定义域必须有限且写死；写不成这个形状就如实说明，不用近似或抽样代替。同时在正文里写明*禁止事项*：不得把 `unknown`（`3`）报成「无反例」，不得把「已测试范围内未发现反例」表述为「无反例」。实现又给这条链加了三道闸：*证据不许覆盖*——`--evidence-out` 指向已存在的路径（或它的 `.sig`）时以 `2` 拒绝；*来源要有凭据*——产证据的命令写盘时自动签出旁挂的 `<path>.sig`，没有签名器就删掉刚写下的那份并以 `4` 退出，台账只认带可信签名的证据（哈希是自证的，`verdict` 只是文件里的一行字）；*写入是事务式的*——先把改动全部应用到副本，再校验*改完的整条记录*（结论与证据种类、对象、范围、输入、来源五项绑定逐一对得上），任何一处不自洽就整笔拒绝（`2`），一个字段都不写。

`opl-prove` 复述证明循环：`Plan → Work → Checkpoint → Review → Replan → Stop`。它把 `lake env lean <file>` 用于文件级门、`lake build` 只在检查点使用，并强制每次验证输出 `#print axioms` 结果。

`opl-evolve` 要求研究者写死骨架、模型只填一个函数，可进化区域用 `EVOLVE-BLOCK-START/END` 标记框死；同时要求评估器的确定性、纯函数、可在无网络沙箱重跑。实现把其中两条收紧成硬约定：评估器必须*两阶段*——`extract` 那一侧有候选、`verify` 那一侧没有候选，两者跑在两个独立的沙箱进程里，只给候选改内存的机会被堵死在进程边界上；评估器的退出码还要与它写下的指标互相自洽（`0` 跑完 / `1` 按约定不可行 / `2` 拒收候选 / `≥3` 自身故障，故障时它的指标一概不采信），两者矛盾时不下判决，返回 `UNKNOWN`（`verdict_conflict`）。

`opl-benchmark` `[未实现]`。固化统计路径而不是暴露「自己挑一个检验」的开关：校正后正态性检验，再 Bartlett 或 Levene 方差齐性，再配对 t 或 Wilcoxon 符号秩，再多组 rmANOVA 加 Tukey 或 Friedman 加 Nemenyi；校正方法与随机种子写入记录的分析字段。

`opl-report` `[未实现]`。约束报告的生成方式：报告只读 `CSV` / `JSON` / `YAML`，不内嵌计算；台账视图是生成物，不是人工维护的第二份真源。

== 入口技能与清单字段

清单中需要显式列出的字段：

#table(
  columns: (auto, 1fr),
  table.header([*字段*], [*取值与理由*]),
  [`skills` `[已实现]`], [`["./skills/opl-entry/", "./skills/opl-refute/", "./skills/opl-formalize/", "./skills/opl-prove/", "./skills/opl-evolve/"]`——实现里的 `kimi.plugin.json` 就是这样逐个列出这五个目录的。必须显式列出：省略该字段会进入 root-skill-only 模式，只认根 `SKILL.md`],
  [`sessionStart` `[已实现]`], [`{skill: "opl-entry"}`。参照 `superpowers` 的真实用法],
  [`mcpServers`], [*本项目不声明*。能力以 `bin/` 下的可执行文件提供，不注册 `MCP` 服务器；仅当某些环境确需 `MCP` 入口时才加一个 0 到 2 个工具的薄适配层],
)

清单不声明 `mcpServers`，因此 `bin/` 下的命令靠 `KIMI_PLUGIN_ROOT` 定位（插件自身用 `OPL_BINDIR` 覆盖，便于测试）。权限上不需要 `MCP` 工具名，直接用 `ToolName(arg-pattern)` 形式的规则约束命令行，例如按前缀 allow 或 deny `Bash(opl-*)`。

调研确认清单内*没有任何* `${PLUGIN_ROOT}` 类占位符，运行期只注入环境变量 `KIMI_PLUGIN_ROOT` 与 `KIMI_CODE_HOME`。也不存在官方 JSON Schema 校验（`$schema` 指向的地址实测返回官网页面而非 schema），`name` 字段是唯一必填且唯一致命项，其余字段问题一律降级为诊断。

因此本项目不使用 Claude 生态的 `hooks/hooks.json` 形态。Kimi 的 hooks 是扁平数组 `{event, matcher, command, timeout}`，且为 fail-open（非零退出、超时、崩溃均放行），只能用于告警，不能充当安全边界。真实约束应写进权限规则的静态拒绝项。

== 同步状态

截至插件 `0.6.0`（版本真源是 `kimi.plugin.json`）：设计中的七个技能落地五个（`opl-entry` / `opl-refute` / `opl-formalize` / `opl-prove` / `opl-evolve`），对应里程碑 M0–M4 且都有回归覆盖；`opl-benchmark`（M5）与 `opl-report`（M6）未实现。因此本章的技能表、正文要点与清单字段三处都按「五个已实现加两个未实现」标注，未实现的条目留在原位、只加状态标记。这五个技能的目录名、清单的 `skills` 列表与 `sessionStart.skill` 的挂载已逐条核对一致；正文要点里的签名层、台账事务校验与两阶段评估器是原计划未设想、由实现补上的机制。
