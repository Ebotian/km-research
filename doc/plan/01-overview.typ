= 项目概览

本项目的交付物是一个*本地安装*的 Kimi Code 插件，代号 `open-problem-lab`，用于协助研究者攻击开放问题：
数学猜想的形式化与证明搜索、反例搜索、算法上界改进、以及算法设计与实证基准评测。

插件的形态由平台决定，不由我们设计。官方插件的实际结构是清单、技能，加一个可选的 `MCP` 服务器。本项目在第三项上做了一个刻意的选择：*不写单体服务器，而是写一组单一职责的可执行文件*，理由与代价见「工具链与组合契约」章。因此实际结构是四件：

- `kimi.plugin.json`（插件根清单，单文件）
- `skills/<name>/SKILL.md`（技能：告诉 agent 何时做、按什么判据做）
- `bin/opl-*`（单一职责命令：判决走退出码，`stdout` 只出机器可解析内容）
- `lab/`（运行时数据：台账、证据、运行快照）

这四件之外还有一个不被平台直接引用的实现层 `lib/opl_*.py`（12 个模块：共享契约 `opl_common`、台账 `opl_ledger`、签名 `opl_sign`、能力探测 `opl_probe`、编码 `opl_encode`、搜索 `opl_search`、证书复核 `opl_certcheck`、运行与沙箱 `opl_run` / `opl_sandbox`、Lean `opl_lean`、进化 `opl_evolve`、规格 `opl_spec`），`bin/` 下的命令都是它的薄壳。

调研阶段（见附录）覆盖 24 个专题、共约 4600 行 Typst，本节只给出被调研证据支持的结论性判断。

== 研究工作的真实瓶颈

调研中反复出现的失效模式可以归为三类，它们决定了插件的设计边界：

- *形式化鸿沟*。自动形式化的编译率会系统性高估语义忠实度：statement 自动形式化的语义正确率（TC%）约 76%，在低覆盖度设定下答案正确率仅 20%。因此「Lean 编译通过」不能作为「陈述已正确形式化」的证据。
- *验证器缺位*。AI Scientist v1 的复现研究观察到 42% 实验因编码错误失败、多篇生成论文含幻觉数值与残留占位符；其在无外部反馈时无法自我纠错。
- *伪报成功*。Erdős 问题的公开案例中，Gemini 的「自主解决」经复核降级为独立重发现（Erdős-935，6 降到 5）；2025-10 的「GPT-5 解决 10 个 Erdős 问题」经查是误报，因为站点的 `open` 标签本身不保证最新。

三类失效的公共对策是同一条：让独立验证器对结论拥有否决权，并把「证据等级」做成数据结构里的一等字段。

== 六个核心设计判断

以下判断贯穿全部章节，后续设计不再重复论证。

#table(
  columns: (auto, 1fr),
  table.header([*判断*], [*依据与后果*]),
  [工具链不调用大模型],
  [变异算子留给宿主 agent，可执行文件只做「数据库 + 评估器 + 沙箱」。插件零 API key、换模型不改工具链、agent 可把 Lean / Julia / Typst 直接揉进搜索循环。],
  [验证器否决权],
  [所有结论必须经与生成路径不同的代码复核。冲突时以验证器为准，`unknown` 绝不折叠为「已证」。],
  [退出码即判决],
  [`0` 通过 / `1` 推翻 / `2` 用法 / `3` 未知 / `4` 缺必需件（后端或签名器）/ `5` 空结果。`unsat` 与 `unknown` 因此在*数值上*无法混淆，空结果也不可能伪装成通过；`1` 只在主动推翻时出现，「读不懂」只能走 `3`。实测补充一句：退出码只表达*这一步是否完成*，研究判决看结构化字段与档位——`0` 不等于「结论已被独立复核」。],
  [能力探测而非硬依赖，且按解释器探测],
  [后端分散在 pacman 与 PyPI 两侧（`z3` 属前者，`cvc5` / `ortools` / `python-sat` 属后者），曾出现*没有任何单一解释器同时看得见两者*的分裂——每一侧单独看都正常。`decompress` 则是「存在但坏了」。因此探测必须同时覆盖可执行文件与 Python 模块、按解释器分别探，并在某一层无解释器能满足时报 `split_brain`；后端按「存在即启用、缺失则降级并显式宣告不可解」处理，绝不静默回退到假结果。],
  [候选代码隔离执行],
  [候选程序在独立进程组（`bwrap` 或 `systemd-run --user --scope`）中运行，禁网、限内存、限 CPU、限写盘。本机 `ulimit` 全为 `unlimited`，必须自己设。],
  [一记录一文件],
  [台账与证据是纯文本、一记录一文件，可直接 `diff`、`grep`、`jq`、入版本控制。参照实测：某 `MCP` 服务器 38 个工具约 13,448 tokens 每请求，而命令行工具的定义成本为零。],
)

== 成功判据

设计完成的判据必须是可执行的，而不是「功能齐全」这种形容词。本插件的验收标准如下：

+ *可安装*：`/plugins install` 后技能出现在 `Extra` 段，`sessionStart` 入口技能自动注入，`bin/` 下的命令可被执行。
+ *可复现*：任一实验目录 `lab/runs/<run_id>/` 内含 `cmd.json`、`env.json`、`capabilities.json`、`metrics.json`，第三方在相同机器上重跑得到同一结论；`run_id` 由输入内容哈希决定，重跑幂等。
+ *可证伪*：反例搜索的结果必须三态输出，`unknown` 不得报为 `unsat`；每条 `REFUTED` 必须携带可独立复核的证书文件。
+ *可审计*：任一「新结果」在报告前必须经过一次文献检索步骤，并把检索式写入台账；台账条目带来源 URL 与归属。
+ *不伪报*：缺失验证器（或缺失签名器）时硬失败，不允许回退到估算值；空结果走独立退出码 `5`，与「通过」区分。`4` 因此也覆盖「没有签名器」：找不到 `ssh-keygen` 或本机没有密钥时，证据产出命令宁可删掉刚写下的那份，也不留下无签名的文件。

=== 当前实现状态

同步状态：截至插件 `0.6.0`，里程碑 M0–M4 已实现并纳入回归，M5（`opl-stat` + `opl-benchmark`）与 M6（`opl-report`）未实现；命令 12 个、技能 5 个，`plugin/scripts/regress.sh` 167 项全通过（无 Lean 时 151 通过 / 16 跳过）、`plugin/scripts/verify-zip.sh` 56 项。

M0 的垂直切片全部通过实测，用来验证组合契约本身成立：

#table(
  columns: (auto, 1fr),
  table.header([*命令*], [*已验证的行为*]),
  [`opl-capabilities` `[已实现]`], [探测必需层与证明层；`decompress` 经*功能*探测被判为 `broken`（上游 bug，见风险章）；按解释器分别探测 Python 模块——项目 venv 13/13、系统 `python3` 8/13，只给系统解释器时正确报出 `split_brain` 并逐项列出缺失],
  [`opl-certcheck` `[已实现]`], [真证书 `0`；翻转一个文字 `1`；空证书 `3`；坏解压器 `3`（*未*误报为 `1`）；缺后端 `4`，缺签名器同样 `4`；`R_4_4_18`（153 变量 / 6120 子句 / 8.4 MB `.bz2`）`0`，460 毫秒],
  [`opl-conj` `[已实现]`], [状态变更无 `--evidence` 时退出码 `2` 拒绝写入；派生状态与历史记录正确；无匹配查询返回 `5`；没有签名器时写台账同样返回 `4`],
)

*命令集*（`bin/`，12 个，全部 `[已实现]`）：`opl-capabilities`、`opl-conj`、`opl-encode`、`opl-search`、`opl-certcheck`、`opl-leancheck`、`opl-run`、`opl-evolve-init` / `-suggest` / `-eval` / `-show`、`opl-sign`；原计划的 `opl-stat` 与 `opl-report` `[未实现]`。*技能集*（`skills/`，5 个，全部 `[已实现]`）：`opl-entry`、`opl-refute`、`opl-formalize`、`opl-prove`、`opl-evolve`，由 `kimi.plugin.json` 的 `skills` 显式列出、`sessionStart.skill` 指向 `opl-entry`；原计划的 `opl-benchmark` 与 `opl-report` 技能 `[未实现]`。原计划是 13 个命令与 7 个技能，实到 12 与 5，差异是多了 `opl-sign`（原计划没有它）、少了 `stat` 与 `report`。

*实现中超出原计划的机制*（均 `[已实现]`）：

+ *签名层*（`opl-sign` + `lib/opl_sign.py`）。用系统自带 OpenSSH 的 `ssh-keygen -Y sign` / `-Y verify`（SSHSIG，命名空间 `open-problem-lab`）。记录是*内嵌签名*：记录 JSON 顶层的 `signature` 字段，签的是去掉它之后的规范序列化，于是「写入 + 签名」是一次原子提交；证据是*旁挂签名*（`<path>.sig`，由验证器命令一次写出）。它挡的是「手写一份自洽的假证据」——哈希是自证的，`verdict` 只是文件里的一行字。*信任边界*：密钥在本机 `~/.config/open-problem-lab/ledger_ed25519`（`OPL_SIGNING_KEY` 可覆盖），沙箱只挂显式列出的文件，因而看不到它；但同一用户下有 shell 的进程仍能读到密钥、照样签——它降低的是自欺的概率，不是不可伪造。
+ *台账的事务式校验*（`lib/opl_ledger.py` 的 `apply_changes` / `validate_record`）。先把改动全部应用到副本，再校验*改完的整条记录*（结论与证据种类/判决、对象、范围、档位、被盖章的反例是否自洽），不自洽就*整笔拒绝、一个字段都不写*。
+ *证据不许覆盖*。`--evidence-out` 指向已存在的路径（或它的 `.sig`）时以 `2` 拒绝——台账记录按哈希引用证据，覆盖会让引用它的记录直接失效；`4` 同时覆盖「没有签名器」，此时刚写下的那份证据会被删掉，不留下无签名的文件。
+ *评估器的退出码契约*。评估器 `0` 跑完（可行性看实验定义里的 `feasible` 字段）、`1` 按约定不可行、`2` 拒收候选、`≥3` 自身故障（*不采信*它写的指标）；*退出码与指标互相矛盾*时返回 `UNKNOWN`（标签 `verdict_conflict`）。判决只有一处实现，`opl-evolve-init` 与 `opl-evolve-eval` 共用。
+ *`opl-evolve-init` 的事务*。先在 sibling 暂存目录 `.evolve-new-<ts>` 里把整个新实验建完（含基线评估与库），再整体切换；旧实验整目录归档到 `<lab>/.evolve-bak-<ts>`；切换之后不再有任何写操作。
+ *运行索引与成绩来源*。`evaluations.run_dir` *相对实验目录*存储（读出时解析），归档后索引跟着归档走；`programs` 多一列 `metrics_evaluation_id`——当前头条指标是哪一次运行跑出来的，成绩跟着产生它的那次评估走（失败运行只追加历史，不动归属），来源不明的成绩被排除并单独计数（`skipped_unattributed`）。

== 范围与非目标

*范围内*：猜想台账、反例搜索、Lean 形式化与证明验证、进化式程序搜索、实验溯源 `[已实现]`；算法基准测试与统计、Typst 报告生成 `[未实现]`（即里程碑 M5 与 M6）。

*范围外*（每一条都有调研依据）：

- *不做本地大模型*。本机 12 核 / 30 GiB，本地 loogle 建索引峰值约 13 GiB，会与 Mathlib 构建抢内存；671B 级 prover 只能走远端。
- *不做论文写作流水线*。AI Scientist 的写作与自动评审环节实测不可靠（自产 7 篇一律拒稿、人类论文误判 9/10），且本项目的产出应是机器可检查的（验证器退出码、可复现脚本、结构化 JSON）。
- *不抓取 `erdosproblems.com`*。该站无 API、无导出，`robots.txt` 显式屏蔽主流 AI 爬虫，且其 FAQ 自述状态不保证最新。
- *不自研文献检索*。`arxiv-mcp-server` 等已覆盖约 20 个数据源，本机 `uvx` 可用，外挂即可。
- *不引入重型实验管理框架*。`DVC` / `Snakemake` / `MLflow` / `W&B` 分别解决数据版本化、DAG 调度、多运行查询的问题，当前需求尚未出现；先做扎实 `Git` + 锁文件 + `manifest.json`。

== 与既有资产的关系

调研与实测确认了几处可以直接复用、不应重写的资产：

#table(
  columns: (auto, 1fr, auto),
  table.header([*资产*], [*用途*], [*关系*]),
  [`drat-trim` / `lrat-check`],
  [命题 CNF 的 UNSAT 证书独立复核；`drat-trim` 亦提供二进制证明格式],
  [*包装，不改源码*。上游两处 bug 由插件侧规避，见风险章],
  [`kimi-datasource/bin/*.mjs`],
  [564 行零依赖 stdio `MCP` 骨架，含 `initialize` / `tools/list` / `tools/call` / `ping` 最小实现],
  [仅当需要 `MCP` 适配层时参考],
  [`arxiv-mcp-server`],
  [arXiv 检索与正文分页读取，默认 12k 字符分段],
  [外挂],
  [`lean-lsp-mcp`],
  [Lean 目标查询、多 tactic 尝试、`build` 反馈、公理白名单],
  [参考其命名与切分],
  [`lean4-skills`],
  [`Plan → Work → Checkpoint → Review → Replan → Stop` 状态机与 `/命令` 划分],
  [照搬分层],
)
