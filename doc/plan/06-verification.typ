= 验证纪律

这一章是插件的宪法。前面各章描述「怎么做」，本章描述「什么情况下不允许说自己做成了」。

== 五级证据阶梯

每条结论必须携带 `verification_level`，取值只有五个，且必须说明依据：

#table(
  columns: (auto, 1fr),
  table.header([*等级*], [*含义与判据*]),
  [`empirical`], [仅由有限域搜索或有限次实验支持。「在已测试范围内未发现反例」属于这一级],
  [`exact_certificate`], [携带可独立复核的证书：`DRAT` 证明、精确有理等式、区间算术包围盒。复核脚本与生成路径不同],
  [`lean_checked`], [Lean 内核接受，且公理白名单通过、无 `sorry`、未使用 `native_decide`],
  [`human_peer_reviewed`], [经同行评审或署名专家确认],
  [`UNVERIFIED`], [无法验证。这一级只能出现在调研记录里，不能出现在结论中],
)

值得单独强调：`native_decide` 不进入 `lean_checked`。Lean 核心 `Lean/Meta/Native.lean` 的文档自述该机制会向逻辑*新增一条断言公理*，把编译器与 `@[implemented_by]` 定义拉进可信基；`bv_decide` 同样建立在其上。要给开放问题存档的界，必须走 `norm_num` / `linarith` / `bound` 这类产出可检验证明项的路径。

另一条同样重要：白皮书不等于同行评审。AlphaEvolve 至今是 arXiv 预印本（无 journal-ref），而 FunSearch、AlphaTensor、AlphaDev 是经同行评审的 Nature 论文。两者不应在报告里并列同一可信度——报告输出里每条结果都带来源 URL 与等级。

== 三态逻辑

求解器的输出到结论的映射是固定的，不允许模型自行解释：

#table(
  columns: (auto, auto, 1fr),
  table.header([*求解器输出*], [*结论*], [*强制附加内容*]),
  [`sat`], [`REFUTED`], [仅当候选见证经独立复核通过；否则为 `WITNESS_UNCERTIFIED`],
  [`unsat`], [`NO_COUNTEREXAMPLE_IN_RANGE`], [必须写明有限域边界与「仅在该域内」的限定措辞],
  [`unknown`], [`INCONCLUSIVE`], [必须附 `reason_unknown` 原文（如 `memout`、`incomplete`）],
)

`unknown` 折叠为 `unsat` 是本项目最需要防的单点错误。同理，「未在测试范围内找到反例」不得改写为「无反例」。

== 空结果与退出码

程序化组件（`scripts/` 与 `MCP`）的退出码必须区分三种情形，参照 `pytest` 退出码 5 的设计：

- 配置或输入错误：非零，且不回退到估算值。
- 空结果（0 个候选、0 次测试被执行）：独立非零码，与「通过」区分。
- 通过：0。

给 agent 预留「工具不可用就自己估算」的内部 fallback 是被明确禁止的：对照实验表明，改变这个 fallback 的吸引力会直接改变 agent 的违规与投机行为。正确做法是让缺失的验证器触发硬失败，并明确告知 agent「被拦截后唯一可走的路是报告失败」。

== 防自欺清单

以下七条作为 `opl-entry` 的核心内容注入每个会话：

+ 验证器未通过时，`formal_status` 保持不变。
+ 任何「新结果」在报告前必须执行一次文献检索，并把检索式写入台账。依据是两次公开事故：Erdős-935 从自主解决降级为独立重发现；2025-10 的「GPT-5 解决 10 个 Erdős 问题」是误报，因为站点 `open` 标签不保证最新。
+ 不得用同模型、同上下文、同信息集的自评或辩论作为结论依据。无外部反馈时模型无法自我纠错，辩论还会推高双方自信度；辩论只在 critic 强于 judge、且被要求把对方发言当作待验证主张逐条核验时才有显著增益。
+ 形式化状态在人工确认前停在 `compiles`，不写成 `faithfulness_checked`。
+ 报捷前强制插入文献检索步骤，并检查归属。
+ 部分成功必须显式记录。反例搜索应记录*已穷尽排除的约束类及其覆盖率*，而不是只记「失败」。
+ 所有跨时间的比较必须检查元数据一致性；`env` 或工具版本不一致时拒绝合并结果。

== 失败模式记录

失败要按粒度记录，而不是只记最终答案。调研给出两条依据：近半数幻觉轨迹混合多种类型，事后验证会系统性漏检；而把失败模式按轨迹级粒度记录是唯一可靠的做法。

运行记录的 `verdict` 字段与 `runstatus` 枚举共同承担这件事：`timeout` 与 `memout` 不是「失败」，而是删失观测，必须参与生存分析而不是被丢弃。

实证基准的报告口径固定为 $op("pass")^k$（$k$ 取 5 到 8）加成本，而不是单次成功率。FunSearch 在 $n=8$ cap set 上 140 次只有 4 次成功，却在该问题族上每次运行都超过基线——同一系统在不同问题上的方差可以差两个数量级，单次运行不足以支撑任何结论。
