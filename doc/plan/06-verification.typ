= 验证纪律

这一章是插件的宪法。前面各章描述「怎么做」，本章描述「什么情况下不允许说自己做成了」。

*同步状态*：截至插件 `0.6.0`（版本真源是 `kimi.plugin.json`），本章的退出码语义与证据阶梯已随 `lib/` 的 12 个模块、`bin/` 的 12 个命令落地（档位表与判决名的实际取值见各节），里程碑 `M0`–`M4` `[已实现]` 且都有回归覆盖；`M5`（`opl-stat` + `opl-benchmark` 技能）与 `M6`（`opl-report`）`[未实现]`，凡只与它们有关的判据在文中加了这个标记。原计划未曾设想的几道闸门——台账事务式校验、评估器退出码契约、签名层、成绩与评估的绑定——见末节。

== 五级证据阶梯

每条结论必须携带 `verification_level`，取值只有五个，且必须说明依据：

#table(
  columns: (auto, 1fr),
  table.header([*等级*], [*含义与判据*]),
  [`empirical`], [仅由有限域搜索或有限次实验支持。「在已测试范围内未发现反例」属于这一级],
  [`exact_certificate`], [携带可独立复核的证书：`DRAT` 证明、精确有理等式、区间算术包围盒。复核脚本与生成路径不同],
  [`lean_checked`], [Lean 内核接受，且公理白名单通过、无 `sorry`、未使用 `native_decide`],
  [`human_peer_reviewed`], [经同行评审或署名专家确认],
  [`UNVERIFIED`], [无法验证。这一级只能出现在调研记录里，不能出现在结论中],
)

*实现的档位表只有四项*：`lib/opl_ledger.py` 的 `LEVELS` 是 `empirical` / `exact_certificate` / `lean_checked` / `human_peer_reviewed`，`UNVERIFIED` 不在台账接受的档位里——它以 `verified_by` 的取值出现在「没有被证据撑住的反例」上，档位同时落回 `empirical`。原计划把它算作第五个档位，实现把它移出了档位表；差异照实标出，没有按其一改写另一处。

值得单独强调：`native_decide` 不进入 `lean_checked`。Lean 核心 `Lean/Meta/Native.lean` 的文档自述该机制会向逻辑*新增一条断言公理*，把编译器与 `@[implemented_by]` 定义拉进可信基；`bv_decide` 同样建立在其上。要给开放问题存档的界，必须走 `norm_num` / `linarith` / `bound` 这类产出可检验证明项的路径。

另一条同样重要：白皮书不等于同行评审。AlphaEvolve 至今是 arXiv 预印本（无 journal-ref），而 FunSearch、AlphaTensor、AlphaDev 是经同行评审的 Nature 论文。两者不应在报告里并列同一可信度——报告输出里每条结果都带来源 URL 与等级。

== 三态逻辑

求解器的输出到结论的映射是固定的，不允许模型自行解释：

#table(
  columns: (auto, auto, 1fr),
  table.header([*求解器输出*], [*结论*], [*强制附加内容*]),
  [`sat`], [`REFUTED`], [仅当候选见证经独立复核通过；否则为 `WITNESS_UNCERTIFIED`],
  [`unsat`], [`NO_COUNTEREXAMPLE_IN_RANGE`], [必须写明有限域边界与「仅在该域内」的限定措辞],
  [`unknown`], [`INCONCLUSIVE`], [必须附 `reason_unknown` 原文（如 `memout`、`incomplete`）],
)

*实现的判决名与上表不同*，映射本身没变：`opl-search` 的判决行是 `SAT` / `UNSAT` / `WITNESS_INVALID`（求解器判 `sat` 而见证不满足规格 → `1`，即编码或反解码有错），台账的 `formal_status` 取小写 `refuted` / `no_counterexample_in_range` / `inconclusive`。表里的 `WITNESS_UNCERTIFIED` 与 `reason_unknown` 两个名字在代码里不存在：后者对应 `opl-search` 的 `reason` 字段，前者对应台账里反例的 `verified_by = UNVERIFIED`。差异照实标出。

`unknown` 折叠为 `unsat` 是本项目最需要防的单点错误。同理，「未在测试范围内找到反例」不得改写为「无反例」。

== 空结果与退出码

程序化组件的退出码就是判决通道。六个取值各有明确含义，其中两条是本项目的关键防线：

- 配置或输入错误：`2`，且不回退到估算值。`--evidence-out` 指向已存在的路径（或它的 `.sig`）也走这里——证据是一次写出的产物，不许覆盖。
- 空结果（0 个候选、0 次执行）：`5`，独立于「通过」。实测中 `opl-evolve-eval` 的重复提交（`code_hash` 命中）、`opl-evolve-suggest` 库里没有带指标的候选、`opl-conj list` 无匹配，都走这里。
- 通过：`0`。
- *主动推翻*：`1`。只有找到见证或判定证书无效时才允许出现。
- *无法判定*：`3`。格式未识别、解析不完整、超时，全部走这里。
- 缺失后端，以及*没有签名器*：`4`。三个证据产出命令（`opl-certcheck`、`opl-encode --eval-witness`、`opl-leancheck`）找不到 `ssh-keygen` 或没有本机密钥时不但不产出证据，还会删掉刚写下的那一份；写台账（`opl-conj`）同样是 `4`。

*但退出码只回答「这个命令完成了它那一步」，不回答「结论成立吗」。* `opl-search` 在 `unsat` 时返回 `0` 只说明它找到了一份证明，复核是 `opl-certcheck` 的事，而后者可能报 `3`——所以 `0` 从不等于「已被独立复核」。研究判决看结构化字段与档位：台账的 `formal_status` / `verification_level`、证据记录的 `verdict`。（原计划把 `0` 定义为「断言成立，且已被独立复核」，实现把它收窄成「这一步没出错」；差异照实标出。）

`1` 与 `3` 的分离是这套设计的核心。「我读不懂这份证书」与「这份证书是错的」是两件事，把前者报成后者就是伪证。实测确认了这条防线是必要的：上游 `decompress` 有一处 bug 会把合法证书还原成垃圾，下游校验器于是老老实实报 `NOT VERIFIED`——若照单全收，就会把一份好证书判成假的。工具因此必须自己回答「证书被完整读入了吗」，答不上来只能走 `3`。

给 agent 预留「工具不可用就自己估算」的内部 fallback 是被明确禁止的：对照实验表明，改变这个 fallback 的吸引力会直接改变 agent 的违规与投机行为。正确做法是让缺失的验证器触发硬失败，并明确告知 agent「被拦截后唯一可走的路是报告失败」。

== 防自欺清单

以下七条作为 `opl-entry` 的核心内容注入每个会话——*这是原计划*：实现的 `opl-entry` 注入的不是这七条，而是三条不可协商的纪律（`unknown` 绝不折叠、结论必须经与生成路径不同的代码复核、缺失验证器时硬失败）。七条里只有「形式化状态停在 `compiles`」在 `opl-formalize` 里逐字落实，第 1 条由工具行为承担（依据撑不起档位的写入整笔拒绝，状态因此不会被改），其余几条在技能正文里找不到落点。差异照实标出，清单按原样留着：

+ 验证器未通过时，`formal_status` 保持不变。
+ 任何「新结果」在报告前必须执行一次文献检索，并把检索式写入台账。依据是两次公开事故：Erdős-935 从自主解决降级为独立重发现；2025-10 的「GPT-5 解决 10 个 Erdős 问题」是误报，因为站点 `open` 标签不保证最新。
+ 不得用同模型、同上下文、同信息集的自评或辩论作为结论依据。无外部反馈时模型无法自我纠错，辩论还会推高双方自信度；辩论只在 critic 强于 judge、且被要求把对方发言当作待验证主张逐条核验时才有显著增益。
+ 形式化状态在人工确认前停在 `compiles`，不写成 `faithfulness_checked`。
+ 报捷前强制插入文献检索步骤，并检查归属。
+ 部分成功必须显式记录。反例搜索应记录*已穷尽排除的约束类及其覆盖率*，而不是只记「失败」。
+ 所有跨时间的比较必须检查元数据一致性；`env` 或工具版本不一致时拒绝合并结果。

== 失败模式记录

失败要按粒度记录，而不是只记最终答案。调研给出两条依据：近半数幻觉轨迹混合多种类型，事后验证会系统性漏检；而把失败模式按轨迹级粒度记录是唯一可靠的做法。

运行记录的 `verdict` 字段与 `runstatus` 枚举共同承担这件事：`timeout` 与 `memout` 不是「失败」，而是删失观测，必须参与生存分析而不是被丢弃。*这一段有一半 `[未实现]`*：已落地的是 `opl-run` 的结局分类（`timeout` / `oom` / `oom_inferred` / `signal`，其中 `oom` 有内核 `oom_kill` 记账作硬证据，`oom_inferred` 只有「死在 deadline 之前且被杀」的推断，快照里如实区分两者），未落地的是名为 `runstatus` 的枚举与生存分析本身——那属于 `opl-stat`（M5）。名字也有出入：计划里写 `memout`，实现里是 `oom` / `oom_inferred`。

实证基准的报告口径固定为 $op("pass")^k$（$k$ 取 5 到 8）加成本，而不是单次成功率。*这条口径的落点是基准统计，属 `opl-stat`（M5，`[未实现]`）*：现有命令只把单次运行如实记下来，聚合与删失统计还没有实现。FunSearch 在 $n=8$ cap set 上 140 次只有 4 次成功，却在该问题族上每次运行都超过基线——同一系统在不同问题上的方差可以差两个数量级，单次运行不足以支撑任何结论。

== 原计划未设想、实现补上的闸门

下面六道闸门不在任何里程碑里，是实测与逐轮审阅逼出来的。它们的共同性质是：把「不自洽就拒绝」做成工具行为，而不是提示词里的一句告诫。

=== 台账的事务式校验

改动先全部应用到一份副本，再校验*改完的整条记录*（结论 ↔ 证据种类与判决、对象、范围、档位、被盖章的反例），任何一处对不上就整笔拒绝、一个字段都不写（`2`）。防的是「检查绑定在这回传了哪些参数上」——只改范围、只升档、塞一份别的见证来加反例，都能从没传参数的那一侧绕过去；为此每次写入还要*重新加载*证据本身，核对它的哈希与它引用的输入文件的哈希，因为摘要不是证据（把被验证的见证换掉、路径不变，只读摘要就查不出来）。

=== 证据是一次写出的产物

`--evidence-out` 指向已存在的路径（或它的 `.sig`）时以 `2` 拒绝。防两件事：一是产出流程「先写正式文件、再签名」遇上签名器不可用时会把刚写下的那份删掉，同一路径写第二次就会连着先前那份有效证据一起毁掉；二是台账按哈希引用证据，悄悄换掉同一路径上的内容会让那条记录直接核不过。要重做就先显式删掉旧的——那个 `rm` 就是「我知道旧的那份将被丢弃」的确认动作。

=== 实验目录的整体切换

`opl-evolve-init` 先在 sibling 暂存目录 `.evolve-new-<ts>` 里把整个新实验建完（含基线评估与程序库），再整体切换；归档的是*整个旧实验目录*（`<lab>/.evolve-bak-<ts>`），提交之后不再有任何写操作。防的是「建到一半失败，活实验被改成一个新旧混合的半成品」——失败时删掉暂存即可，活实验一个字节都没动。

=== 评估器退出码契约

实验的评估器有自己的协议退出码：`0` 跑完（可行性看实验定义里的 `feasible` 字段与值）、`1` 按约定=不可行（这是结论）、`2` 拒收这个候选、`≥3` 是它自身故障（*它写的指标一律不采信*）。更硬的一条是*退出码与指标矛盾时不下判决*：退出码说不可行而指标说可行、或退出码说跑完而指标说不可行，都返回 `UNKNOWN`（标签 `verdict_conflict`），把两份原始信息一起留下，而不是任选一个当依据。判决只有一处实现，`opl-evolve-init` 的基线评估与 `opl-evolve-eval` 共用它，所以基线不会走一条更松的路。

=== 签名层

证据与记录都签（`opl-sign` + `lib/opl_sign.py`，用系统自带 OpenSSH 的 `ssh-keygen -Y sign/verify`，SSHSIG，命名空间 `open-problem-lab`）。防的是最省事的一种造假：*手写一份自洽的假证据*——写文件的人同时写了被引用的文件，于是哈希全对，而 `verdict` 只是文件里的一行字、没有任何东西验过它。哈希证明「现在这份字节是什么」，签名证明「这份字节是谁写的」，两件互不替代。落盘方式刻意不同：*证据旁挂* `<path>.sig`（验证器命令一次写出），*记录内嵌*在 JSON 顶层的 `signature` 字段、签的是去掉它之后的规范序列化，于是「写入 + 签名」是一次原子提交——签不成，盘上那条旧记录一个字节都不会动。

*信任边界*：密钥在本机 `~/.config/open-problem-lab/ledger_ed25519`（`OPL_SIGNING_KEY` 可覆盖），沙箱只挂显式列出的文件，所以沙箱里的候选与评估器看不到它；但同一用户下有 shell 的进程（包括宿主 agent）仍能读密钥、照样签。它把「写个 JSON」升级成「动一次密钥」——降的是自欺的概率、涨的是事后可复查性，不是不可伪造。

=== 成绩与产生它的那次评估绑定

`programs` 多一列 `metrics_evaluation_id`：*当前头条指标是哪一次运行跑出来的*。防的是「失败运行洗成绩」——超时、评估器崩了这类运行不写指标却会追加一行历史，若拿「最近一次运行」当身份，一次超时补测就能把旧定义的成绩洗成新定义的。失败运行只追加历史，不动归属；来源查不到的成绩被排除在比较之外并单独计数，而不是拿别的运行的指纹替它作证。运行索引 `evaluations.run_dir` 按*相对实验目录*存储、读出时解析，于是归档之后索引跟着归档走，不会再指向活实验。
