"""排序网络骨架：写死的部分在外，模型只能改 EVOLVE-BLOCK 里的比较器表。

这是 `opl-evolve` 的玩具问题。选排序网络而不是在线装箱，理由是它的评估器
**确定性且无自由参数**：按 0-1 原理，一个比较器网络能排序所有输入，当且仅当它能
排序所有 2^n 个 0-1 序列。装箱要先定一个输入分布，那就多一个可以争论的自由度，
而「改进」这种事最不该留可以争论的空间。

可进化区就是 `build_network` 返回的那张表。骨架给的是冒泡网络（n=4 要 6 个比较器），
n=4 的最优是 5 个——所以「严格优于骨架」是有真实余量的，不是刻意留的假缺口。
"""

N = 4

# EVOLVE-BLOCK-START
def build_network() -> list[tuple[int, int]]:
    """比较器序列：每个元素是 `(i, j)`，表示比较 `a[i]` 与 `a[j]` 并按序交换。

    骨架：冒泡/插入式网络，6 个比较器。n=4 已知最优 5 个。
    """
    return [(0, 1), (1, 2), (2, 3), (0, 1), (1, 2), (0, 1)]
# EVOLVE-BLOCK-END
